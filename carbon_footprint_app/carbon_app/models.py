from django.db import models

# Database models (store user data)
# Need to make migrations every time you change models.py 
# py -m manage makemigrations, then py -m manage migrate
# Prevents data loss
# Create your models here.
